/* 
    FUNCTIONS
    reusable action use case

    Terminology
    function body
    function addNumbers() {
        js code runs
    }

    calling a function / invoking a function
    nameFn()
    addNumbers(arg, arg, arg)
    addEventListener()
*/

// arg(2)      num1 = 2
function addNumbers(num1, num2) {
    return num1 + num2;
}

// fn() call/ invoking
const sum = addNumbers(2, 3); /* for some reason mine doesn't show up */